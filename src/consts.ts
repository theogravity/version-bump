/**
 * Version bump type
 */
export enum BUMP_LEVEL {
  MAJOR = 'major',
  MINOR = 'minor',
  PATCH = 'patch',
  PRE_MAJOR = 'pre-major',
  PRE_MINOR = 'pre-minor',
  PRE_PATCH = 'pre-patch',
  PRE_RELEASE = 'pre-release',
  BUILD_RELEASE = 'build-release',
  LOWEST = 'lowest'
}
